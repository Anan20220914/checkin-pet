# 导航重构：主界面 + 二三级界面

## Context

部署到 GitHub Pages 后（https://anan20220914.github.io/checkin-pet/），用户反馈两个问题：
1. **"根本不能用"** —— 实测页面能加载，但在 1280px 宽屏下 `#app` 限了 `max-width:480px` 居中、左侧导航 76px + 内容 372px 挤在中间一小条，两侧大片空白，观感像没打开/极小；且旧 service worker 缓存可能让用户看到旧版本。
2. **导航结构不对** —— 当前是"左侧导航 + 平铺所有 25 个打卡任务在一屏"，用户要的是「有主界面，进入各打卡是二三级界面」，不要一屏堆满、不要左侧导航。

目标：把首页改成**入口卡片式主界面**，点各分类进入**二级界面**，学习模块点进去再进入**三级 quiz 浮层**。底部保留 5 个一级 Tab。删除左侧导航。

## 新导航结构

**底部一级 Tab（不变 5 个）**：首页 / 宠物 / 商店 / 战斗 / 设置

**首页（主界面）`view-home`**：
- 顶部：头像 + 今日进度概览环（完成数/总分/积分）
- 入口卡片网格（2 列，大卡片）：
  - 📚 学习打卡（角标：完成 X/4）
  - 🌱 习惯养成（角标：完成 X/7）
  - ⚽ 运动打卡（角标：完成 X/7）
  - 🏠 家务打卡（角标：完成 X/7）
  - ⚔️ 每日对决（宽卡片，显示连胜 + 发起按钮状态）
  - 🎁 周日礼物（宽卡片，显示当前等级 + 倒计时）

**二级界面**（首页内部子页切换，顶部带「‹ 返回」回到主界面）：
- 学习页 `sub-study`：4 个大入口卡片（识字/英语/数学/开口）→ 点击打开三级 quiz 浮层（复用现有 openChineseQuiz/openEnglishQuiz/openMathQuiz/openSpeakBattle）
- 习惯页 `sub-habit`：7 项勾选列表（复用 toggleCheckTask）
- 运动页 `sub-sport`：勾选列表
- 家务页 `sub-life`：勾选列表

三级 quiz 浮层逻辑（识字/英语/数学/开口）**完全不变**，复用现有 `views/quiz-*.js`。

## 技术实现

### 路由：首页内部子视图状态
不引入新依赖，在 `views/checkin.js` 内部用一个 `subView` 变量管理（'home' | 'study' | 'habit' | 'sport' | 'life'）。`renderCheckin()` 根据 subView 渲染对应内容。点入口卡片设 subView 后重渲染；子页「返回」按钮设回 'home'。

### 关键文件改动

1. **`js/views/checkin.js`**（重写，核心）
   - 删除 `.checkin-layout` + `#sideNav` 左侧导航结构
   - `renderCheckin()` 改为根据 `subView` 分发到 `renderHome()` / `renderStudy()` / `renderHabit()` / `renderSport()` / `renderLife()`
   - `renderHome()`：进度环 + 入口卡片网格 + 每日对决卡片 + 周礼物卡片
   - `renderStudy()`：4 个学习模块大入口（识字/英语/数学/开口），点击调现有 `openChineseQuiz` 等
   - `renderHabit/Sport/Life()`：对应分类任务的勾选列表（复用 `getTasksByCategory` + `toggleCheckTask`）
   - 每个子页顶部带返回按钮 `setSubView('home')`
   - 复用现有：`getTodayProgress`、`getStudyDoneCount`、`isDailyDuelDone`、`getTasksByCategory`、`toggleCheckTask`、`isTaskDone`、`getQuizResult`、`currentGiftPreview`

2. **`js/app.js`**
   - `renderCurrentView()`：checkin tab 渲染调 `renderCheckin()`（已是这样，但要确保子页切换时 store 广播后重渲染仍停在当前 subView —— subView 是模块级变量，store 广播重渲染会保持，OK）
   - 每日对决卡片点击跳战斗 tab：复用 `switchTab('battle')`

3. **`css/style.css`**
   - 删除/保留 `.side-nav` `.checkin-layout` `.checkin-main` 相关样式（可删，因不再用）
   - 新增 `.home-grid`（2 列卡片网格）、`.home-tile`（入口大卡片：图标 + 名称 + 角标）、`.sub-header`（子页返回栏）、`.sub-task`（子页勾选项，复用 `.task` 样式即可）
   - 调整 `#app` 宽度：`max-width: 480px` 保留（手机优先），但桌面端可加一个居中背景，避免空白突兀（可选美化）

### 不动的部分
- 数据层 `store.js/db.js/tasks.js`、SRS、宠物、商店、战斗、成就、周礼物、图鉴、同步、PWA —— 全部不动
- quiz 浮层 `views/quiz-chinese.js/quiz-english.js/quiz-math.js/quiz-speak.js` —— 不动，复用
- 宠物/商店/战斗/设置 view —— 不动

## 验证方式

1. 本地 `python -m http.server 8888 --bind 127.0.0.1`，浏览器强刷（Ctrl+F5 清旧 SW 缓存）
2. 用 mcp browser 打开，确认：
   - 首页显示入口卡片网格，无左侧导航
   - 点"学习打卡"进学习子页，显示 4 个模块入口；点"识字"打开识字 quiz 浮层；完成返回，角标更新
   - 点"习惯养成"进习惯页，勾选项生效，返回首页角标更新
   - 点"每日对决"跳战斗页
   - 各 Tab（宠物/商店/战斗/设置）正常
3. 改 `#app` max-width 在 1280px 下观感不畸形
4. 重新打包 zip，让用户重新上传 GitHub（或指导用户更新仓库文件后等 Pages 重新部署 + 强刷）
